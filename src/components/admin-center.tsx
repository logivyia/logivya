import { Activity, AlertTriangle, CheckCircle2, CircleDollarSign, Database, FileText, ShieldCheck, Users } from "lucide-react";
const icons=[Activity,Users,ShieldCheck,CircleDollarSign,FileText,Database,AlertTriangle,CheckCircle2];
export function AdminCenter({eyebrow,title,description,metrics,children}:{eyebrow:string;title:string;description:string;metrics:Record<string,string|number>;children?:React.ReactNode}){return <><header className="mb-7"><p className="text-xs font-semibold uppercase tracking-[.2em] text-orange-600">{eyebrow}</p><h1 className="mt-2 text-3xl font-semibold">{title}</h1><p className="mt-2 text-sm text-slate-500">{description}</p></header><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{Object.entries(metrics).map(([label,value],i)=>{const Icon=icons[i%icons.length];return <div key={label} className="rounded-2xl border bg-white p-5 shadow-sm"><Icon className="mb-5 size-5 text-orange-500"/><p className="text-xs text-slate-500">{label}</p><p className="mt-1 text-2xl font-semibold">{value}</p></div>})}</div>{children&&<div className="mt-6">{children}</div>}</>}
export function AdminTable({headers,rows,emptyLabel="-"}:{headers:string[];rows:(string|number|null|undefined)[][];emptyLabel?:string}){return <div className="overflow-x-auto rounded-2xl border bg-white p-5 shadow-sm"><table className="w-full text-sm"><thead><tr className="border-b text-left text-xs text-slate-500">{headers.map(x=><th className="py-3" key={x}>{x}</th>)}</tr></thead><tbody>{rows.map((row,i)=><tr className="border-b last:border-0" key={i}>{row.map((x,j)=><td className="py-4 pe-4" key={j}>{x??"-"}</td>)}</tr>)}</tbody></table>{!rows.length&&<p className="py-10 text-center text-sm text-slate-500">{emptyLabel}</p>}</div>}
