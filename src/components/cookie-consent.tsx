"use client";/* eslint-disable react-hooks/set-state-in-effect */import { useEffect,useState } from "react";
export function CookieConsent(){const[open,setOpen]=useState(false),[manage,setManage]=useState(false);useEffect(()=>setOpen(!localStorage.getItem("logivya.cookie-consent")),[]);function save(value:string){localStorage.setItem("logivya.cookie-consent",value);setOpen(false)}if(!open)return null;return <div className="fixed inset-x-4 bottom-4 z-[100] mx-auto max-w-3xl rounded-2xl border bg-white p-5 shadow-2xl"><h2 className="font-semibold">Cookie tercihleri</h2><p className="mt-2 text-sm text-muted">Zorunlu cookie&apos;ler oturum ve güvenlik için kullanılır. Analitik ve pazarlama cookie&apos;leri tercihinize bağlıdır.</p>{manage&&<div className="mt-4 grid gap-2 text-sm"><label><input checked disabled type="checkbox" className="me-2"/>Zorunlu</label><label><input name="analytics" type="checkbox" className="me-2"/>Analitik</label><label><input name="marketing" type="checkbox" className="me-2"/>Pazarlama</label></div>}<div className="mt-4 flex flex-wrap gap-2"><button onClick={()=>save("all")} className="rounded-xl bg-primary px-4 py-2 text-sm font-semibold text-white">Tümünü kabul et</button><button onClick={()=>save("essential")} className="rounded-xl border px-4 py-2 text-sm">Zorunlu olmayanları reddet</button><button onClick={()=>setManage(v=>!v)} className="rounded-xl border px-4 py-2 text-sm">Tercihleri yönet</button></div></div>}
