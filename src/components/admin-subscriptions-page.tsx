"use client";
import { FormEvent, useCallback, useEffect, useState } from "react";
import { LoaderCircle } from "lucide-react";
type Company={id:string;name:string;email?:string;owner:{email:string};billingProfile?:{legalName?:string};subscriptions:Array<{status:string;plan:{name:string}}>} ;
const field="rounded-xl border bg-white px-3 py-3 text-sm",panel="rounded-2xl border bg-card p-6";
export function AdminSubscriptionsPage(){const[companies,setCompanies]=useState<Company[]|null>(null),[status,setStatus]=useState("");const load=useCallback(()=>fetch("/api/admin/companies").then(async r=>{const value=await r.json();if(!r.ok)throw new Error(value.error);setCompanies(value.companies)}),[]);useEffect(()=>{void load().catch(e=>setStatus(e.message))},[load]);async function activate(event:FormEvent<HTMLFormElement>){event.preventDefault();const form=new FormData(event.currentTarget);const r=await fetch("/api/admin/subscriptions/manual-activate",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(form))});const value=await r.json();setStatus(r.ok?"Abonelik, ödeme ve taslak fatura oluşturuldu.":value.error);void load()}return <><header className="mb-7"><p className="text-xs font-semibold uppercase tracking-[.2em] text-primary">Platform Yönetimi</p><h2 className="mt-2 text-3xl font-semibold">Manuel Abonelik Aktivasyonu</h2></header>{status&&<p className="mb-4 rounded-xl border p-4 text-sm">{status}</p>}<form onSubmit={activate} className={`${panel} mb-6 grid gap-4 md:grid-cols-3`}><select required name="companyId" className={field}><option value="">Şirket seçin</option>{companies?.map(c=><option key={c.id} value={c.id}>{c.name} · {c.owner.email}</option>)}</select><select name="planSlug" className={field}><option value="starter">Başlangıç</option><option value="professional">Profesyonel</option><option value="enterprise">Kurumsal</option></select><select name="billingPeriod" className={field}><option value="MONTHLY">Aylık</option><option value="YEARLY">Yıllık</option><option value="CUSTOM">Özel dönem</option></select><input required name="startsAt" type="datetime-local" className={field}/><input required name="endsAt" type="datetime-local" className={field}/><select name="paymentMethod" className={field}><option value="MANUAL_BANK_TRANSFER">Banka transferi</option><option value="CASH">Nakit</option><option value="OTHER">Diğer</option></select><input name="currency" defaultValue="TRY" className={field}/><input name="note" placeholder="Not" className={field}/><button className="rounded-xl bg-primary px-4 py-3 font-semibold text-white">Manuel etkinleştir</button></form><section className={panel}>{!companies?<LoaderCircle className="animate-spin"/>:<div className="overflow-x-auto"><table className="w-full text-sm"><thead><tr className="border-b text-left"><th className="py-3">Şirket</th><th>Fatura profili</th><th>Mevcut abonelik</th></tr></thead><tbody>{companies.map(c=><tr key={c.id} className="border-b"><td className="py-4"><b>{c.name}</b><p className="text-xs text-muted">{c.owner.email}</p></td><td>{c.billingProfile?.legalName||"Eksik"}</td><td>{c.subscriptions[0]?`${c.subscriptions[0].plan.name} · ${c.subscriptions[0].status}`:"-"}</td></tr>)}</tbody></table></div>}</section></>}
