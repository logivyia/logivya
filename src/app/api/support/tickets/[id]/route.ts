import { NextResponse } from "next/server";import { requireApiSession } from "@/server/auth/session";import { prisma } from "@/server/db";
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){try{const{company}=await requireApiSession(),{id}=await params;const ticket=await prisma.supportTicket.findFirst({where:{id,companyId:company.id},include:{messages:{where:{isInternal:false},include:{senderUser:{select:{name:true}}},orderBy:{createdAt:"asc"}}}});return ticket?NextResponse.json({ticket}):NextResponse.json({error:"NOT_FOUND"},{status:404})}catch{return NextResponse.json({error:"UNAUTHORIZED"},{status:401})}}
