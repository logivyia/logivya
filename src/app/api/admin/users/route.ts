import { NextResponse } from "next/server";import { requirePlatformAdmin } from "@/server/auth/platform-admin";import { prisma } from "@/server/db";
export async function GET(request:Request){try{await requirePlatformAdmin("users:manage",request);const p=new URL(request.url).searchParams,page=Math.max(1,Number(p.get("page")||1)),q=p.get("q")||"";const where=q?{OR:[{name:{contains:q,mode:"insensitive" as const}},{email:{contains:q,mode:"insensitive" as const}},{phone:{contains:q,mode:"insensitive" as const}}]}:undefined;const[users,total]=await Promise.all([prisma.user.findMany({where,select:{id:true,name:true,email:true,phone:true,status:true,locale:true,timezone:true,country:true,createdAt:true,memberships:{include:{company:{select:{name:true}}}},sessions:{where:{revokedAt:null},orderBy:{lastActiveAt:"desc"},take:5},trustedDevices:{where:{revokedAt:null},take:5},platformAdmin:true},orderBy:{createdAt:"desc"},skip:(page-1)*30,take:30}),prisma.user.count({where})]);return NextResponse.json({users,pagination:{page,total,pages:Math.ceil(total/30)}})}catch{return NextResponse.json({error:"FORBIDDEN"},{status:403})}}
