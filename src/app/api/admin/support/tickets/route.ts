import { NextResponse } from "next/server";import { requirePlatformAdmin } from "@/server/auth/platform-admin";import { prisma } from "@/server/db";
export async function GET(request:Request){try{await requirePlatformAdmin();const p=new URL(request.url).searchParams,page=Math.max(1,Number(p.get("page")||1)),status=p.get("status");const where=status?{status:status as never}:undefined;const[tickets,total]=await Promise.all([prisma.supportTicket.findMany({where,include:{company:{select:{name:true}},createdBy:{select:{name:true,email:true}},assignedToAdmin:{select:{name:true}},messages:{orderBy:{createdAt:"desc"},take:1}},orderBy:{lastMessageAt:"desc"},skip:(page-1)*30,take:30}),prisma.supportTicket.count({where})]);return NextResponse.json({tickets,pagination:{page,total,pages:Math.ceil(total/30)}})}catch{return NextResponse.json({error:"FORBIDDEN"},{status:403})}}
