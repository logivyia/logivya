import { NextResponse } from "next/server";import { requirePlatformAdmin } from "@/server/auth/platform-admin";import { prisma } from "@/server/db";import { writeAuditLog } from "@/server/security/audit";
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){try{const{user}=await requirePlatformAdmin(),{id}=await params;const company=await prisma.company.update({where:{id},data:{securityStatus:"ACTIVE",campaignsPausedAt:null}});await writeAuditLog(request,{companyId:id,userId:user.id,action:"company.admin_reactivated",entityType:"Company",entityId:id});return NextResponse.json({company})}catch{return NextResponse.json({error:"FORBIDDEN"},{status:403})}}
