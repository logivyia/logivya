import { NextResponse } from "next/server";import { requirePlatformAdmin } from "@/server/auth/platform-admin";import { prisma } from "@/server/db";
export async function GET(request:Request){try{await requirePlatformAdmin("admin.billing.read", request);const p=new URL(request.url).searchParams,page=Math.max(1,Number(p.get("page")||1));const[invoices,total]=await Promise.all([prisma.invoice.findMany({include:{company:{select:{name:true}}},orderBy:{createdAt:"desc"},skip:(page-1)*30,take:30}),prisma.invoice.count()]);return NextResponse.json({invoices,pagination:{page,total,pages:Math.ceil(total/30)}})}catch{return NextResponse.json({error:"FORBIDDEN"},{status:403})}}
