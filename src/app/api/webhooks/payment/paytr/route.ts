import { NextResponse } from "next/server";import { receivePaymentWebhook } from "@/server/billing/webhook-handler";export async function POST(request:Request){const result=await receivePaymentWebhook("PAYTR",request);return NextResponse.json(result.body,{status:result.status})}
