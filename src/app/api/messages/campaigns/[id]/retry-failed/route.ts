import { NextResponse } from "next/server";import { requirePermission } from "@/server/auth/permissions";import { requireApiSession } from "@/server/auth/session";import { prisma } from "@/server/db";import { messageQueue } from "@/server/queues/client";import { writeAuditLog } from "@/server/security/audit";
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){try{const{id}=await params,{company,membership,user}=await requireApiSession();requirePermission(membership.role,"retry_campaigns");const campaign=await prisma.messageCampaign.findFirst({where:{id,companyId:company.id,deletedAt:null},select:{id:true}});if(!campaign)return NextResponse.json({error:"NOT_FOUND"},{status:404});const recipients=await prisma.messageRecipient.findMany({where:{campaignId:id,status:"FAILED"},select:{id:true}});await prisma.$transaction([prisma.messageRecipient.updateMany({where:{campaignId:id,status:"FAILED"},data:{status:"PENDING",failedAt:null,errorMessage:null}}),prisma.messageCampaign.update({where:{id},data:{status:"QUEUED",failedCount:0}})]);const queue=messageQueue();for(const recipient of recipients)await queue.add("send-recipient",{companyId:company.id,campaignId:id,recipientId:recipient.id},{jobId:`retry-${recipient.id}-${Date.now()}`});await writeAuditLog(request,{companyId:company.id,userId:user.id,action:"campaign.failed_retried",entityType:"MessageCampaign",entityId:id,after:{recipientCount:recipients.length}});return NextResponse.json({ok:true,count:recipients.length})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"errors.generic"},{status:403})}}
