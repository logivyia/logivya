import { NextResponse } from "next/server";import { requireApiSession } from "@/server/auth/session";import { prisma } from "@/server/db";
export async function GET(request:Request){try{const{company}=await requireApiSession(),p=new URL(request.url).searchParams,page=Math.max(1,Number(p.get("page")||1)),action=p.get("action"),entityType=p.get("entityType");const where={companyId:company.id,...(action?{action:{contains:action,mode:"insensitive" as const}}:{}),...(entityType?{entityType}:{})};const[logs,total]=await Promise.all([prisma.auditLog.findMany({where,include:{user:{select:{name:true,email:true}}},orderBy:{createdAt:"desc"},skip:(page-1)*30,take:30}),prisma.auditLog.count({where})]);return NextResponse.json({logs,pagination:{page,total,pages:Math.ceil(total/30)}})}catch{return NextResponse.json({error:"UNAUTHORIZED"},{status:401})}}
