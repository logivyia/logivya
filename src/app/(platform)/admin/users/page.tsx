import { AdminCenter,AdminTable } from "@/components/admin-center";import { requirePlatformAdmin } from "@/server/auth/platform-admin";import { prisma } from "@/server/db";
export default async function Page(){await requirePlatformAdmin("users:manage");const[users,active,sessions,admins]=await Promise.all([prisma.user.findMany({include:{memberships:{include:{company:{select:{name:true}}},take:1},sessions:{where:{revokedAt:null},orderBy:{lastActiveAt:"desc"},take:1},platformAdmin:true},orderBy:{createdAt:"desc"},take:100}),prisma.user.count({where:{status:"ACTIVE"}}),prisma.userSession.count({where:{revokedAt:null,expiresAt:{gt:new Date()}}}),prisma.platformAdmin.count({where:{isActive:true}})]);return <AdminCenter eyebrow="Identity Governance" title="User Management Center" description="Kullanıcı, oturum, cihaz ve yönetici rollerini privacy-aware biçimde yönetin." metrics={{"Toplam kullanıcı":users.length,"Aktif kullanıcı":active,"Aktif oturum":sessions,"Platform admin":admins}}><AdminTable headers={["Kullanıcı","Şirket","Durum","Dil / timezone","Son aktif oturum","Admin rolü"]} rows={users.map(u=>[`${u.name} · ${u.email}`,u.memberships[0]?.company.name,u.status,`${u.locale} · ${u.timezone}`,u.sessions[0]?.lastActiveAt.toLocaleString(),u.platformAdmin?.role])}/></AdminCenter>}
