import { requirePlatformAdmin } from "@/server/auth/platform-admin";export default async function Page(){await requirePlatformAdmin();return <><h1 className="text-3xl font-semibold">Backup ve Disaster Recovery</h1><p className="mt-2 text-sm text-muted">Managed backup durumları sağlayıcı entegrasyonu tamamlandığında burada görüntülenecek.</p><div className="mt-6 grid gap-4 md:grid-cols-3">{["Son veritabanı yedeği","Son dosya yedeği","Restore hazırlığı"].map((x,i)=><div className="panel rounded-2xl p-5" key={x}><b>{x}</b><p className="mt-3 text-sm text-muted">{i===2?"Runbook hazır":process.env.BACKUP_STORAGE_PROVIDER?"Sağlayıcı yapılandırıldı":"Sağlayıcı bekleniyor"}</p></div>)}</div></>}
