import { AdminCenter,AdminTable } from "@/components/admin-center";import { requirePlatformAdmin } from "@/server/auth/platform-admin";import { prisma } from "@/server/db";
export default async function Page(){await requirePlatformAdmin("platform:read");const rows=await prisma.notification.findMany({include:{company:{select:{name:true}},user:{select:{email:true}}},orderBy:{createdAt:"desc"},take:100});return <AdminCenter eyebrow="Operational Awareness" title="Admin Notifications" description="Kritik ödeme, sistem, güvenlik ve abonelik bildirimlerinin görünümü." metrics={{"Gösterilen":rows.length,"Okunmamış":rows.filter(x=>!x.isRead).length,"Security":rows.filter(x=>x.type.includes("SECURITY")).length,"Billing":rows.filter(x=>x.type.includes("PAYMENT")||x.type.includes("INVOICE")).length}}><AdminTable headers={["Tür","Başlık","Şirket","Kullanıcı","Okundu","Tarih"]} rows={rows.map(x=>[x.type,x.title,x.company.name,x.user.email,x.isRead?"Evet":"Hayır",x.createdAt.toLocaleString()])}/></AdminCenter>}
