import { AdminCenter,AdminTable } from "@/components/admin-center";import { requirePlatformAdmin } from "@/server/auth/platform-admin";import { prisma } from "@/server/db";
export default async function Page(){await requirePlatformAdmin("data_requests:manage");const rows=await prisma.dataSubjectRequest.findMany({include:{company:{select:{name:true}},user:{select:{email:true}}},orderBy:{requestedAt:"desc"},take:100});return <AdminCenter eyebrow="Privacy Operations" title="Data Requests" description="Erişim, export, düzeltme ve silme taleplerini yönetin." metrics={{"Toplam":rows.length,"Bekleyen":rows.filter(x=>["REQUESTED","VERIFYING","PROCESSING"].includes(x.status)).length,"Tamamlanan":rows.filter(x=>x.status==="COMPLETED").length,"Silme":rows.filter(x=>x.type==="DELETION").length}}><AdminTable headers={["Tür","Durum","Şirket","Kullanıcı","Talep tarihi"]} rows={rows.map(x=>[x.type,x.status,x.company?.name,x.user?.email,x.requestedAt.toLocaleString()])}/></AdminCenter>}
