import { AdminCenter,AdminTable } from "@/components/admin-center";import { requirePlatformAdmin } from "@/server/auth/platform-admin";import { prisma } from "@/server/db";
export default async function Page(){await requirePlatformAdmin("operations:read");const rows=await prisma.messageCampaign.findMany({include:{company:{select:{name:true}},createdBy:{select:{email:true}}},orderBy:{createdAt:"desc"},take:100});return <AdminCenter eyebrow="Mesaj Operasyonları" title="Campaigns" description="Platform genelindeki kampanya sağlığını ve başarısızlıkları izleyin." metrics={{"Gösterilen":rows.length,"Başarısız":rows.filter(x=>x.status==="FAILED").length,"Gönderiliyor":rows.filter(x=>x.status==="SENDING").length,"Tamamlanan":rows.filter(x=>x.status==="COMPLETED").length}}><AdminTable headers={["Kampanya","Şirket","Durum","Sent / Failed","Aktör","Tarih"]} rows={rows.map(x=>[x.title,x.company.name,x.status,`${x.sentCount} / ${x.failedCount}`,x.createdBy.email,x.createdAt.toLocaleString()])}/></AdminCenter>}
