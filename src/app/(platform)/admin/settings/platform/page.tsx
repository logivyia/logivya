import { AdminCenter } from "@/components/admin-center";import { requirePlatformAdmin } from "@/server/auth/platform-admin";
export default async function Page(){await requirePlatformAdmin("operations:manage");return <AdminCenter eyebrow="Platform Governance" title="Platform Settings" description="Bakım modu, yönetici bootstrap ve operasyon sağlayıcılarının güvenli görünümü." metrics={{"Maintenance":process.env.MAINTENANCE_MODE==="true"?"Enabled":"Disabled","Email provider":process.env.EMAIL_PROVIDER||"Not configured","Backup provider":process.env.BACKUP_STORAGE_PROVIDER||"Not configured","Initial admin":process.env.INITIAL_PLATFORM_ADMIN_EMAIL?"Configured":"Not configured"}}/>}
