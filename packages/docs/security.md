# Security Operations

Private routes authenticate sessions; tenant routes filter every query by company; admin routes require `PLATFORM_ADMIN_EMAILS`. Critical actions create immutable audit logs. Secrets, raw API keys, passwords, tokens, and WhatsApp session data must never be logged.
